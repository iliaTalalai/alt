<% if user_signed_in? %>
  <h1> Welcome <%= current_user.name %> </h1>
<% else %>
  <div class="hero-unit">
    <h1>All The Little Things...</h1>
    <p>A space for celebrating aesthetics</p>
    <p>
       <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
    </p>
  </div>
<% end %>